

.) save share our work
    > option file - export - save for web (alt + shift + ctr + S)
    > option file - export - export as (alt + shift + ctr + W) => save on multiple sizes
    > right click on layer - quick export as PNG  => past way to save in PNG [change png to jpg => edit-preferences-export-quick export formate ]
    > right click on layer - export as => selected layer will save
    > option file-generate-image asset => search it most important

.) Image Resize
    ctr + t or alt + ctr + I

.) canvas size => ctr + alt + c

.) open or close all layer group
    hold ctr and click on group layer

.) link layer => sath kil layer
    select layers and on layer panel bottom size on icon appear (link layer) click on it.

.) copy layer or group 
    hold alt and drag layer => automatically copy layer or group

.) select layer with same color content
    select one hold ctr and shift then select same color content.   

.) option view - show - smart guides
.) show/hides guides => ctr + ; => ctr + r se aata hai ruller 
.) lock/unlock guides => alt +  ctr + ; 
    
.) opacity vs fill => check with stroke property

.) shape layer => right click select Resterize layer.
.) smart object => does not loose quality. => right click an convert into smart object.

.) auto select => top bar 
.) show transform controls => top bar 

.) layer filter => layer pallet top side 4 options

.) merge all group layer ==> alt + ctr + shift + E

.) image face blur => filter - blur- surface blur

.) Replace tshirt color => image - adjustments - replace color

.) shave psd in less size => smart object layer  should be Resterize layer

.) for father => shift + F5

.) expand selection => select - modify- expand

.) anti-alias checked => before marquee tool select  - top bar me option